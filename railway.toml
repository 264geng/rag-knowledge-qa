[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "cd backend && python create_test_data.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/auth/me"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
